# チェンジログ

このプロジェクトのすべての注目すべき変更は、このファイルに記録されています。

## [1.7.0] - 2026-09-xx

### 追加

- サムネイル右上に`綴じ方向L or R` `ソート条件 S` `指定ページサムネイル T`この3条件の保存状況を知らせるようにした

### 修正

- ローディング時や拡大縮小時のちらつきを抑制
- アーカイブ単位でソート順の保存と復帰ができるようにした。ビューアーの右クリックメニューからアクセス可能
- アーカイブのサムネイルを任意のページで指定できるようにした。ビューアーの右クリックメニューからアクセス可能
- アプリ起動時の復帰フォルダとツリービューを同期するようにした
- お気に入りマークとサムネイル読み込み失敗のマーク表示がかぶらないように修正
- 見開きオフセット操作時に仮想ページ仮想ページの組み合わせがアーカイブファイル先頭と最後にできていたのを修正。破壊的なページ移動をする状態ではオフセット操作ボタンを無効化するようにした
- お気に入りと検索においてデータベースの登録に一貫性が損なわれるカレント形式で結果を書き込んでいたのを修正
- オフセット操作ボタンを押したときのページ遷移アニメーションがおかしかったのを修正

## [1.6.0] - 2026-08-22

### 追加

- Linux用AppimageをReleasesに再度追加
- アイテムペインでのフォルダ移動操作をフォルダツリーペインに反映させるようにした
- サムネイルアイテム欄でマウスD&Dによる画面スクロールを追加した

#### サムネキャッシュ済みデータ限定のファイル検索機能を追加

- ファイラーに検索タブを追加。ここから利用できる
- 全体検索だと結果が複雑化するのでカレントフォルダー＋そのサブディレクトリをオプションで追加する形にした。サブディレクトリを含めるかどうかはオプション。この範囲内でサムネ済みファイルのみが検索対象

##### 検索の手順

1. 検索タブを選択する
2. ペインの構成が変わるが、ドライブ一覧とフォルダツリーで検索の基底フォルダを決定する
3. 左端のペインに検索条件があるので設定していく
4. 検索開始ボタンを押す
5. 右側の空欄に検索でヒットした項目が表示される

##### 検索時の注意点

- findやwindows search的な汎用的検索はできない
- 新設したての画像ディレクトリには検索が一切効かない。サムネイルキャッシュ済となったファイルが最低でも1つ以上なければ検索ディレクトリとして認識しない。
- 検索時にサムネイルが無いファイルを検知してもサムネイル化作業は行わない
- 検索履歴は起動セッション毎に初期化される

### 修正

- Flatpak版のマウスアイコンが正常に変わらないのを修正

### 変更

- 設定保存場所はXDG-nekoviewフォルダ/ユーザーディレクトリを正式にした。旧来の設定であるバイナリ横の場合は起動時に選択が出る（設定の削除は手動管理してください）

## [1.5.1] - 2026-08-16

### 変更

- AppImage配布を一旦断念、Flatpakに変更した
- 失敗していたLinux AppImage配布導線を、Freedesktop Runtimeを使うFlatpakパッケージへ置き換えた
- Flatpak sandboxでユーザーコンテンツをRead Onlyにし、設定とキャッシュはアプリ専用XDG領域へ書き込むようにした
- CUIログをすべて既定OFF、ビューアーのリサイズフィルターをLanczos3、基準解像度をウィンドウサイズ追従へ変更した

## [1.5.0] - 2026-08-16

### 追加

- Linux用AppimageをReleasesに追加するようにした

### 変更

- XDG基準, Windowsだと%appdata%以下を状態保存先としてデフォルト化。nekoviewerフォルダを作成しそこに設定ファイルを入れ込む。
- バイナリ横保存設定の場合から切り替えるときにマイグレーション機能を実装した。マイグレーション後の消去はconfだけ行なう。その他はユーザー側で消してください

## [1.4.4] - 2026-08-07

### 修正

- フィルター文字列への入力が阻害される状態を修正
- 古いデコード結果を表示してモアレやボケが発生していたのを修正

## [1.4.2]

### 変更

- 翻訳機能を実験的機能から昇格させた

### 修正

- お気に入り作成ダイアログが正しく動作していなかったのを修正

## [1.4.0] - 2026-07-13

### 追加

- 実験機能:ローカルLLMとの接続でOCR〜翻訳機能を追加
- キーアサイン機能追加。完全ではないので継続して作業中

### 修正

- 設定の項目が正しく保存・復帰できていなかったのを修正
- サムネイルを表示枠と同じアスペクト比に伸長し表示していたのを修正。オリジナルのアスペクト比を守るようにした

## [1.3.0] - 2026-07-09

### 追加

- サムネイルグリッド内のフォルダアイテム対応
- フルキーボードオペレーション対応
TABキー,shift+TABでエクスプローラー内フォーカスの移動＋カーソルキー、ENTER,ESCでおおよその主要オペレーションが可能
(サブメニューアクセスは非対応)

### 修正

- ネットワークドライブ、特にSMBでのアクセスの遅延修正


## [1.2.0] - 2026-07-07

### 追加

#### お気に入り機能の追加

- お気に入りはフォルダツリー上部にタブを新設した、そこからお気に入りフォルダにアクセス可能
- お気に入りアイテム設定はエクスプローラー部、ビューアー部ともに右クリックメニューで可能。エクスプローラー部では単独ファイルならマウスホバー位置直下のアイテムが対象。複数選択時はホバー位置は関係せず、選択されたものが対象となる
- １ファイルにつき、複数のお気に入りフォルダに紐付けできる。作者別、純粋なお気に入りなどと同時に活用可能
- ネットワークドライブの先の物とローカルストレージのものを混在させる時は注意

### 変更

- 多重起動をしないようにした

### 修正

- アーカイブ内のアニメーションファイルの再生スピードがおかしくなっていたのを修正
- 設定画面内でスクロールバーが表示せず、閉じる・適用ボタンまで視覚的に到達できないのを修正


## [1.1.0] - 2026-07-05

### 追加

#### HOME / END キーの機能実装

- エクスプローラー部ではサムネセレクターを先頭・最後尾に移動させる
- ビューアー部ではアーカイブファイル内の現在地を先頭・最後尾に移動させる

#### ビューアー部で右クリックメニューから現在の見開き設定を保存できる

- シングルページモードは保存できないが、上書き保存でシングルページモードもメニュー選択は出来るようにはなっている。セーブなしのデフォルト値に戻すだけのアクション
- 綴じ方向、オフセット値も保存される
- ページ位置は保存せずファイル先頭で状態復元される。オフセット値は破壊しないような復元をするので位置補正用の仮想ページが現れることがあるがバグではない
- データベースファイルは実行ファイル横に配置される。初期化したいときは消してください

#### サムネイルバーにページ番号の追加

#### アーカイブ形式の追加対応

- TAR / CBT（無圧縮・gzip圧縮）に対応。`.tar` / `.cbt` / `.tgz` / `.tar.gz`
- TAR + zstd圧縮（`.tar.zst` / `.tzst`）に対応。純Rust実装でC依存の追加なし

#### 画像形式の追加対応

- TIFF / TIF に対応（純Rust実装）。マルチページTIFFは1ページ目のみ表示、CCITT G3/G4やJPEG-in-TIFFなど一部の圧縮方式は非対応

### 修正

- 拡張子のファイルタイプ判定からファイルの冒頭のバイナリ列をみてデコードや展開を行うようにした。これによりデコードできないファイルに遭遇したときのフォールバック不可バグの修正もあわせてできている
- サムネイルバーの表示が重かったのを修正

### 検討中

- RAR形式アーカイブ対応
- お気に入りフォルダ対応：お気に入りファイルを仮想お気に入りフォルダに格納していく仕様
- 拡大縮小機能

## [1.0.0] - 2026-07-04

### 追加
- ビューアー部のアーカイブ内サムネイル表示の追加
- これに伴うビューアー設定の追加, 非表示も可能
- エクスプローラー部下部にファイル名検索機能を追加
文字列で検索できるようになった。blob形式で*や?の文字も使用可能
チェックボックスがフィルタ有効無効のキーとなっている
この文字列は起動セッション内のみ永続し、次の起動までは持ち越さない

### 修正
- waylandでのフルスクリーン時のページ送り挙動の修正
ウィンドウ自体の更新が止まり停止したようにみえてしまう問題。autonovsyncフラグを建てるようにした
- 設定画面のレイアウト修正

## [0.9.0] - 2026-07-03

### 追加
- 7zファイル対応（パスワード付きは未実装）

### 変更
- できるだけターミナルが出ないように変更、ログはGUIから確認できるようにした。

## [0.8.0] - 2026-07-02

### 追加
- 設定GUIの追加

### 変更
- アニメーションにはリングバッファを適用。省メモリ実現

### 修正
- アニメーションにスローがかかっていたのを修正

## [0.7.0] - 2026-06-30

### 追加
- 多言語対応 : 切り替えメニューをサムネイル表示部のメニューバー右側に配置した(ja/en/cn)
- (主にwindows)F5 から F8キーのウィンドウサイズ / ポジション 記憶復帰機能にデフォルト番号をconfにて割り振れるようにした

### 変更
- キャッシュの最適化

### 変更 (開発メモ)
- 基本構造をMVCモデル化
- マルチウィンドウの更新不具合でeframeの除外

### 既知の問題
- キャッシュメモリあふれ時の表示不具合、特に巨大なアニメーションファイルの対応。もしくは表示しきれない場合のフォールバック
- ローディング表示の明確化
- 設定ファイルの多言語化

### 作業中
- 検索機能
- アーカイブ内移動中のサムネイルと移動マーカー

## [0.6.1] - 2026-06-27

### 追加
- `←` / `→` キーでのファイルジャンプ: 現在のページ位置に関係なく、前後のファイルの1ページ目に直接ジャンプします

### 変更
- RAMキャッシュは、ファイルごとに縦スクロールと横スクロールの領域に分けて管理するようになりました。ファイル間のブラウジング時のキャッシュ効率が向上します

## [0.5.0] - 2026-06-26

### 追加
- ディレクトリナビゲーションがシングルクリック化されました（[直]ボタンは削除されました）
- 無効なZIPマーカーキャッシュ: 画像を含まないZIPは`.neko/invalid/`の下にマークされます。ZIPが更新されると自動的にマーカーは削除されます
- 無効なZIPはグリッド内に赤いXで表示され、ダブルクリック時にトースト通知（3秒後に自動消滅）が表示されます
- ビューアで前後にナビゲートする際、無効なZIPは黙ってスキップされます
- サムネイル保存がJPGの個別ファイルから単一のデータベースファイルにリファクタリングされました

### 変更
- ディレクトリツリーの拡張は▶をクリックすることでのみ実行されるようになりました（アイテムをシングルクリックするとディレクトリ内に移動します）

### 修正
- ビューアは画像エントリを含まないZIPに対して開かなくなりました。無効としてキャッシュされると、エクスプローラーから非表示になります

### 進行中
- UI多言語対応（i18n）のサポート

## [0.4.0] - 2026-06-26

### 追加
- Shift+スクロール / Shift+↑↓でのファイルナビゲーション: 最初/最後のページにいるときに前後のファイルに移動します
- Linux ローカルビルドサポート: `setup.sh`と`Makefile`を追加し、ガイド付きの依存パッケージインストール機能を提供します

### 修正
- Linuxビルドが再び利用可能になりました（既知の問題から削除されました）

### 変更
- READMEは明確な初回およびアップデートビルド手順で更新されました

## [0.3.1] - 2026-06-25

### 修正
- GitHub Actionsリリースが正しくEXEファイルを含むようになりました

## [0.3.0] - 2026-06-25

### 追加
- AVIF形式のサポート（静止画とアニメーション）
- アニメーション対応: GIF、AVIF、WebP（APNGはサポートされていません）

### 修正
- スタンドアロンファイルとアーカイブファイル間で同じ名前のキャッシュ衝突を修正
- キャッシュは、ソースファイルがキャッシュされたバージョンより新しい場合に再生成されます

### 既知の問題
- 中央マウスボタンクリックはフルスクリーン切り替えを実行するために軽いマウス移動が必要です
- Linuxビルドは一時的に利用できません

## [0.2.0] - 2026-06-25

### 追加
- 初期リリース
- 自動化されたWindowsビルドとリリースのためのGitHub Actionsワークフロー
